.groups
  - current_user.groups.each do |group|
    = link_to group_path(group) do
      .group
        %p.group__group-name
          = group.name
        %p.group__group-message
          メッセージはまだありません。
